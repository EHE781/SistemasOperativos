#include    <stdio.h>

int main() {
    int COMMAND_LINE_SIZE;
    COMMAND_LINE_SIZE = 6;
    char line[COMMAND_LINE_SIZE];
    while(scanf(line)){
        if(line=="exit" && line == "^D"){
            salir del shell
        }
    }
}